-- ============================================================================
-- FoodPicker 픽업 마감 시간 컬럼 추가 (2026-07-23)
--   픽업 가능 시간 범위(pickup_start / pickup_end) 개념을
--   "주문 후 N분 이내 방문" 단일 값으로 변경.
--   기존 컬럼은 하위 호환을 위해 보존(nullable).
-- 재실행 안전.
-- ============================================================================

ALTER TABLE public.products
  ADD COLUMN IF NOT EXISTS pickup_deadline_minutes INT;
